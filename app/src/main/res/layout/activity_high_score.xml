<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.kaushiksamba.game.HighScore"
    android:background="@drawable/introbg"
    >
<!--
    <TextView
        android:text="@string/zero"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="80sp"
        android:layout_centerInParent="true"
        android:id="@+id/high_score_text"
        />
-->

    <GridView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/scores_gridView"
        android:numColumns="2"
        android:verticalSpacing="10dp"
        />
<!--
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/share_scores_button"
        android:text="SHARE SCORE"
        android:layout_above="@+id/reset_high_score_button"
        android:layout_below="@id/scores_gridView"
        android:onClick="ShareScore"
        />
-->
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:textSize="40sp"
        android:layout_centerHorizontal="true"
        android:text="RESET HIGH SCORES"
        android:id="@+id/reset_high_score_button"
        />

</RelativeLayout>
